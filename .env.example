# TallyDekho Desktop — local overrides (copy to .env — do not commit)
#
# Windows Desktop → Mac backend (hardcoded default in util/backendConfig.js):
# BACKEND_URL=http://192.168.29.243:3001
#
# Note: in ELECTRON_DEV, 127.0.0.1 / localhost are rejected and remapped to the Mac LAN default.
