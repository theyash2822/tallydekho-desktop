# TallyDekho Desktop App — Environment Variables
# Dev backend URL default lives in util/backendConfig.js (update there on IP change).
# Optional override (takes precedence over backendConfig.js):
# BACKEND_URL=http://YOUR_LOCAL_IP:3001

# Production builds use PROD_BACKEND_URL in util/backendConfig.js
