# TallyDekho Desktop App — Environment Variables
# Desktop uses util/helper.js for base URL — update directly

# For local testing (already set in util/helper.js):
# BASE_URL=http://YOUR_LOCAL_IP:3001

# For production (update in util/helper.js before building .exe):
# BASE_URL=https://api.your-domain.com
