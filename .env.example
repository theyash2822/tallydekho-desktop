# TallyDekho Desktop — local overrides (do not commit)
# Copy to .env and set your machine backend:
# BACKEND_URL=http://127.0.0.1:3001
# For LAN Desktop testing against a Mac host, set BACKEND_URL to that host:port.
